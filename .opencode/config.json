{
    "orchestrator": {
        "mode": "pipeline",
        "strict_routing": true
    },
    "permissions": {
        "main_agent": {
            "edit": "deny",
            "bash": "allow"
        }
    },
    "system_prompt_suffix": "CRITICAL: You are an orchestrator router. You cannot write or modify project files yourself. To write code, you MUST invoke the `@coder` sub-agent. To create specs, invoke `@analyst`. To test, invoke `@tester`. To review, invoke `@reviewer`."
}
